{{#if name}}
  <details class="collapse bg-base-100 border-accent border" name={{ name }}>
{{else}}
  <details class="collapse bg-base-100 border-accent border">
{{/if}}
  <summary class="collapse-title font-semibold">{{ title }}</summary>
  <div class="collapse-content text-sm">
    {{{ content }}}
  </div>
</details>
